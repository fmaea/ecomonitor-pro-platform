# Secret key for Flask app and JWT encoding
SECRET_KEY="830511"

# Database URL (e.g., for PostgreSQL: postgresql://user:password@host:port/dbname)
DATABASE_URL="postgresql://830511@host:port/dbname"

# JWT Configuration (Optional - defaults are set in code if not present)
# JWT_ACCESS_TOKEN_EXPIRES_HOURS=1
